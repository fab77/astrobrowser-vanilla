# AstroBrowser

# clone repo:
$ git clone https://github.com/fab77/astrobrowser.git

# move nto astrobrowser folder
$ cd astrobrowser

# install required dev module
$ npm i

# compile the project
$ npm run all

# run the application
$ npm run devstart

# open browser and point to http://localhost:4000
